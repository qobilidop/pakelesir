/* Generated by pakeles from `eth_ipv4_tcp`. Do not edit:
 * regenerate with `pakeles gen c`. */
#include "parser.h"

static uint64_t pk_read_bits(const uint8_t *buf, uint64_t off, uint32_t n) {
  uint64_t v = 0;
  uint32_t i;
  for (i = 0; i < n; i++) {
    uint64_t pos = off + i;
    v = (v << 1) | (uint64_t)((buf[pos >> 3] >> (7 - (pos & 7))) & 1);
  }
  return v;
}

#define PK_S_PARSE_ETHERNET 0
#define PK_S_PARSE_IPV4 1
#define PK_S_PARSE_TCP 2

static int pk_eth_ipv4_tcp_parse_core(const uint8_t *buf, uint64_t bit_len, pk_eth_ipv4_tcp_result_t *out) {
  uint64_t off = 0;
  uint32_t state = PK_S_PARSE_ETHERNET;
  uint32_t depth;
  for (depth = 0; depth < 4u; depth++) {
    switch (state) {
    case PK_S_PARSE_ETHERNET: {
      out->ethernet_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.dst = (uint64_t)pk_read_bits(buf, off, 48);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.src = (uint64_t)pk_read_bits(buf, off, 48);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.ethertype = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      uint64_t key0 = (uint64_t)out->ethernet.ethertype;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_UNSUPPORTED_ETHERTYPE;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_PARSE_IPV4: {
      out->ipv4_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.version = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ihl = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.dscp = (uint8_t)pk_read_bits(buf, off, 6);
      off += 6;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ecn = (uint8_t)pk_read_bits(buf, off, 2);
      off += 2;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.total_len = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.id = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.flags = (uint8_t)pk_read_bits(buf, off, 3);
      off += 3;
      if (off + 13 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.frag_offset = (uint16_t)pk_read_bits(buf, off, 13);
      off += 13;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ttl = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.protocol = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.checksum = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.src = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.dst = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      {
        uint64_t vlen = (((uint64_t)out->ipv4.ihl * 4ULL) - 20ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv4.options_bit_off = off;
        out->ipv4.options_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv4.protocol;
      if (key0 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_UNSUPPORTED_IP_PROTOCOL;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_PARSE_TCP: {
      out->tcp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.sport = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.dport = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.seq = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.ack = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.data_offset = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.reserved = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.flags = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.window = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.checksum = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.urgent = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    }
  }
  out->outcome = 1;
  out->reason = PK_R_MAX_DEPTH_EXCEEDED;
  out->consumed_bits = off;
  return 1;
}

int pk_eth_ipv4_tcp_parse(const uint8_t *buf, uint64_t bit_len, pk_eth_ipv4_tcp_result_t *out) {
  pk_eth_ipv4_tcp_result_t zero = {0};
  *out = zero;
  return pk_eth_ipv4_tcp_parse_core(buf, bit_len, out);
}

const char *pk_eth_ipv4_tcp_reason_str(uint16_t reason) {
  switch (reason) {
  case 1: return "out of bounds";
  case 2: return "max depth exceeded";
  case 3: return "no matching select arm";
  case 16: return "unsupported ethertype";
  case 17: return "unsupported ip protocol";
  default: return "";
  }
}
