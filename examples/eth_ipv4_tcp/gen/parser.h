/* Generated by pakeles from `eth_ipv4_tcp`. Do not edit:
 * regenerate with `pakeles gen c`. */
#ifndef PK_ETH_IPV4_TCP_H
#define PK_ETH_IPV4_TCP_H

#include <stdint.h>
#include <stddef.h>

enum { PK_ETH_IPV4_TCP_ACCEPT = 0, PK_ETH_IPV4_TCP_REJECT = 1 };

typedef enum {
  PK_R_NONE = 0,
  PK_R_OUT_OF_BOUNDS = 1, /* "out of bounds" */
  PK_R_MAX_DEPTH_EXCEEDED = 2, /* "max depth exceeded" */
  PK_R_NO_MATCHING_SELECT_ARM = 3, /* "no matching select arm" */
  PK_R_UNSUPPORTED_ETHERTYPE = 16, /* "unsupported ethertype" */
  PK_R_UNSUPPORTED_IP_PROTOCOL = 17, /* "unsupported ip protocol" */
} pk_eth_ipv4_tcp_reason_t;

typedef struct {
  uint64_t dst;
  uint64_t src;
  uint16_t ethertype;
} pk_eth_ipv4_tcp_ethernet_t;

typedef struct {
  uint8_t version;
  uint8_t ihl;
  uint8_t dscp;
  uint8_t ecn;
  uint16_t total_len;
  uint16_t id;
  uint8_t flags;
  uint16_t frag_offset;
  uint8_t ttl;
  uint8_t protocol;
  uint16_t checksum;
  uint32_t src;
  uint32_t dst;
  uint64_t options_bit_off;
  uint64_t options_bit_len;
} pk_eth_ipv4_tcp_ipv4_t;

typedef struct {
  uint16_t sport;
  uint16_t dport;
  uint32_t seq;
  uint32_t ack;
  uint8_t data_offset;
  uint8_t reserved;
  uint8_t flags;
  uint16_t window;
  uint16_t checksum;
  uint16_t urgent;
} pk_eth_ipv4_tcp_tcp_t;

typedef struct {
  uint8_t outcome; /* 0 = accept, 1 = reject */
  uint16_t reason; /* pk_eth_ipv4_tcp_reason */
  uint64_t consumed_bits;
  uint8_t ethernet_present;
  pk_eth_ipv4_tcp_ethernet_t ethernet;
  uint8_t ipv4_present;
  pk_eth_ipv4_tcp_ipv4_t ipv4;
  uint8_t tcp_present;
  pk_eth_ipv4_tcp_tcp_t tcp;
} pk_eth_ipv4_tcp_result_t;

/* Parse `bit_len` bits of `buf` (reject mode). Returns outcome. */
int pk_eth_ipv4_tcp_parse(const uint8_t *buf, uint64_t bit_len, pk_eth_ipv4_tcp_result_t *out);
const char *pk_eth_ipv4_tcp_reason_str(uint16_t reason);

#endif /* PK_ETH_IPV4_TCP_H */
